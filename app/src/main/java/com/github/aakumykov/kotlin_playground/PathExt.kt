package com.github.aakumykov.kotlin_playground

import android.graphics.Path

/*
fun Path.fromUserGesture(userGesture: UserGesture): Path {
    return Path().apply {
        moveTo(userGesture.fromX, userGesture.fromY)
        lineTo(userGesture.toX, userGesture.toY)
    }
}
*/
